Tasks
1) Create an EventTicketingSystem
    class EventTicketingSystem
    attributes
        name of event: String
        single ticket cost: int|double
        number of attendees
        cost to host event
        revenue generated
    actions:
        default constructor
        3-args constructor:
            name of event
            single ticket cost
            cost to host event
        buytickets(numberOfTickets)
            increase number of attendees
            increase revenu generated
        refundtickets(numberOfTickets)
            opposite of buytickets
        code getters and setters

Task 2)
Create a class named WorldStats
    attritubtes
        Continents: array of Strings
        populationPerContinent: array of ints
        numberOfContinents
        indexTracker
    actions
        1-arg constructor: num continents
        addContinent(String name, int population)
        getContinentByName(String name) => population of continent

Practice Tasks
Practice 1)
Create Employee class
    attributes
        employeeId, name, salary
Create a Company class
    attributes
        numEmployees, array Employees
    methods
        getLowestSalary() => returns lowest employee salary
        getHighestSalary() => returns highest employee salary

Practice 2)
Create Product class
    attributes
        productId, name, price
Create Store class
    attributes
        numbProducts, array of Products,
    methods
        getProductByName(name) => price of Product
        getTotalValueOfProducts => sum of all Product prices


